# PR #10 single-page evidence excerpt

See [the bounded result](../../../docs/PR10_SINGLEPAGE_REGRESSION.md).
`observations.json` contains selected original diagnostic rows and the complete
termination summary from the owner-approved main-build round. It is not a full
journal or a formal acceptance receipt. Counters contain no keystroke text or key
material. Raw local files and isolated trial data remain at the report's local path;
no test database, Keychain material, screenshots or machine-specific launch config
is included here. Preserve historical attempts separately.

The reusable [page](../../../Scripts/fixtures/secure-input-test.html) is byte-identical
to the page used for this completed round. Opening it does not create a password
field; starting a new host trial is a separate coordinated operation.
